/**
 * Marketplace Scheduler Config 5.0
 */

export const MarketplaceSchedulerConfig = {
  worldTickMs: 1000,
  weatherTickMs: 5000,
  seasonTickMs: 15000,
  economyTickMs: 3000,
  communityTickMs: 2000
};