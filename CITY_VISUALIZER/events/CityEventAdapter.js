class CityEventAdapter {
    constructor({ stateView }) {
        this.stateView = stateView;
    }

    handle(event) {
        // TODO: map CITY ENGINE events to visual state
    }
}
module.exports = CityEventAdapter;
